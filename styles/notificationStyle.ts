import { createGlobalStyle } from 'styled-components'

const Notification = createGlobalStyle`

.Toastify__toast--default {
    .Toastify__toast-body {
      color: var(--color-background-dark)
    }
}

`

export default Notification
