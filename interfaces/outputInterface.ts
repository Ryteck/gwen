interface OutputInterface {
    id: string | number;
    item: string | number;
    destiny: string;
    quantity: number;
    user: string | number;
    when: Date;
}

export default OutputInterface
